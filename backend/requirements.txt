# smokeping-easy backend — PINNED dependencies (exact versions, never ranges).
# Regenerate deliberately; Dependabot proposes updates one at a time.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
cryptography==44.0.0
bcrypt==4.2.1
slowapi==0.1.9
APScheduler==3.11.0
httpx==0.28.1
python-multipart==0.0.32
tzdata==2024.2
